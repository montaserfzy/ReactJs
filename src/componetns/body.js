import React, {Component} from 'react'
import {LoginPage, SigninPage, HomePage, Header, NotFound, Footer} from "../componetns/index"

import {Route, Switch} from 'react-router-dom'

class Body extends Component {

    render() {
        return (
            <div></div>
        )
    }
}

export default Body;